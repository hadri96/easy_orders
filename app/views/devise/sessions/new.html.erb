<div class="banneruser">
</div>
<div class="ctn">
  <h3>Login</h3>
  <p>Please login to your account.</p>
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  label: false,
                  placeholder: 'Email',
                  input_html: { class: "input" } %>
      <%= f.input :password,
                  required: false,
                  label: false,
                  placeholder: 'Password',
                  input_html: { class: "input" } %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>
    <div class="form-actions">
      <%= f.button :submit, "Login", class: "btn-dark-blue" %>
      <%= link_to "Forgot your password?", new_password_path(resource_name) %>
    </div>
  <% end %>
</div>
<div class="ctn-new-page">
  <hr>
  <div class="icon">
    <i class="fab fa-facebook" id="facebook"></i>
    <i class="fab fa-google" id="google"></i>
  </div>
  <div class="links">
    <p>Don’t have an account? <%= link_to "Register now", new_registration_path(resource_name) %></p>
  </div>
</div>
